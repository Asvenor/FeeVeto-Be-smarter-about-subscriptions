{
  "name": "subkiller",
  "version": "1.0.0-beta.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check script.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
