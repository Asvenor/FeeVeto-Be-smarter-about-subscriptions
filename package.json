{
  "name": "feeveto",
  "version": "2.0.0-beta.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test",
    "catalogue:validate": "node scripts/catalogue-validate.mjs fixtures/catalogue.example.json",
    "catalogue:publish": "node scripts/catalogue-publish.mjs",
    "check": "node --check worker.js && node --check js/app.js && node --check js/auth.js && node --check js/access.js && node --check js/currencyPreference.js && node --check js/currencyPage.js && node --check js/formModel.js && node --check js/serviceCatalog.js && node --check js/alternativeProvider.js && node --check functions/_shared/alternatives.js && node --check functions/_shared/catalogue-store.js && node --check functions/api/alternatives/recommendations.js && node --check scripts/catalogue-validate.mjs && node --check scripts/catalogue-publish.mjs && npm run catalogue:validate && node --test && vite build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@clerk/backend": "^3.17.1",
    "@clerk/clerk-js": "^6.31.0"
  }
}
